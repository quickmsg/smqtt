---
sort: 1
---

# 启动webSocket


```
 Bootstrap bootstrap = Bootstrap.builder()
                .websocketPort(8999)
                .isWebsocket(true)
                .build()
                .start().block();
```